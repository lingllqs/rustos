[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]

# [profile.dev]
# panic = "abort" # disable unwinding
#
# [profile.release]
# panic = "abort"

[[bin]]
name = "kernel"
test = false
bench = false
