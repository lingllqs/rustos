[package]
name = "rustos"
version = "0.1.0"
edition = "2024"

[dependencies]
bootloader = "0.9"
volatile = "0.2.6"

[profile.dev]
panic = "abort" # disable unwinding

[profile.release]
panic = "abort"

# rust-analyzer 诊断信息
[[bin]]
name = "rustos"
test = false
bench = false
